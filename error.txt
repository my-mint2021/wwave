1. Пустой alt у картинок

2. Нет нужды в данном случае обворачивать дивом картинку)

3. В transition вместо all прописываем те свойства, которые анимируем, ибо если оставить all , то браузер лишний раз будет проверять     оставшийся свойства

4. Удалять не используемые (пустые) селекторы в css

5. Для градиентов используется background-image , если оставить просто background, то ты за собой тянешь оставшийся ненужные свойства

6. а если в футере будет больше текста, при фикс.высоте текст будет вылезать за его пределы.

7. по БЭМу Задать nav.menu (в header) и дальше от него создавать элементы.

8. нет на странице h1 , значит не может быть и h2

9. незачем усложнять классы) У нас главный блок идет slider, значит будет slider__title, slider__text

10. вот такой контент может быть динамическим.Чтобы подстраховаться, нужно создать див, а внутри него поместить все р без класса, т.к. они унаследуют стили от родителя. 

11. вместо " + " прописываем через :not(first-child \ last-child)

12. отталкиваемся от внешнего элемента, а у нас это ссылка. Значит от тега "а" и идет отступ.

13. округлять множитель высоты строки

14. при написании display: flex , justify-content уже имеет по умолчанию старт.

15 не отрубил оутлайн, уже хорошо, но ты сделал его непрозрачным. Тогда, можно применить псевдокласс :focus , чтоб было понятно попал я ТАБом или нет!!!!!!!

16. картинки фона через инлайн стили

17. любые списки через ul

18. в h1 можно вкладывать тэги

19. модификаторы для иконок по БЭМ

20. волна через svg я не понял-сделал фоном-растягивается не верно вроде
